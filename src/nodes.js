// ------header container------;
const headerContainer = document.getElementById('headerContainer');
const pageTitle = document.getElementById("pageTitle");

// ------home container------;
const homeContainer = document.getElementById("homeContainer");
const tendenciasContainer = document.getElementById("tendenciasContainer");
const categoriesContainer = document.getElementById("categoriesContainer");
const yourListContainer = document.getElementById("yourListContainer");
const tendenciaContainer = document.getElementById('tendenciaContainer');
const homeTrendTitle = document.getElementById('homeTrendTitle');

// ------movie container------;
const movieContainer = document.getElementById("movieContainer");
const movieTitle = document.getElementById('movieTitle');
const movieDescription = document.getElementById('movieDescription');
const movieCategories = document.querySelectorAll('.movieCategories');
const movieRate = document.getElementById('movieRate');
const movieRelatedCategories = document.getElementById('movieRelatedCategories');
const relatedMoviesContainer = document.getElementById('relatedMovies');

// ------category container------;
const categoryContainer = document.getElementById("category-container");
const categoryMoviesContainer = document.getElementById('categoryMoviesContainer');
const categoryTitle = document.getElementById('categoryTitle');
const previewCategoriesContainer = document.getElementById('categoriesList');

// ------Search Container------;
const searchContainer = document.getElementById("search-container");
const searchTitle = document.getElementById('searchTitle');
const searchMoviesContainer = document.getElementById("searchMoviesContainer");

// ------Tendencias Container------;
const trendsContainer = document.getElementById('tendencias-container');
const trendMoviesContainer = document.getElementById('trendMoviesContainer');

// ------footer container------;
const footerContainer = document.getElementById("footerContainer");


// BTNS e Inputs
const formBtns = document.querySelectorAll('.FormButton');
const formInputs = document.querySelectorAll('.FormInput');


const backButtons = document.querySelectorAll(".backBtn");
const inputs = document.querySelectorAll('.inputSearch');
